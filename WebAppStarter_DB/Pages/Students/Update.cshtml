@page "/Student/{id}/Update"
@model WebAppStarter_DB.Pages.Students.UpdateModel
@{
    ViewData["Title"] = "Add Student";
}

<div class="container">
    <h1>Udpate Student</h1>

    <form method="post">

        <div class="row mb-3">
            <label for="firstname" class="col-md-1">Όνομα</label>
            <div class="col-md-6">
                <input asp-for="StudentUpdateDTO.Firstname" class="form-control" />
                <span asp-validation-for="StudentUpdateDTO.Firstname" class="text-danger"></span>
            </div>
        </div>

        <div class="row mb-3">
            <label for="lastname" class="col-md-1">Επώνυμο</label>
            <div class="col-md-6">
                <input asp-for="StudentUpdateDTO.Lastname" class="form-control" />
                <span asp-validation-for="StudentUpdateDTO.Lastname" class="text-danger"></span>
            </div>
        </div>

        <div>
            <div class="row mb-3">
                <div class="offset-md-1 col-md-3">
                    <button type="Submit" class="btn btn-primary">Submit</button>
                </div>
            </div>
            <div class="row mb-3">
                <div class="offset-md-1 col-md-3">
                    <a class="btn bnt-outline-primary" href="/Student/getall">Cancel</a>
                </div>
            </div>
        </div>
    </form>

    <div>
        @if (Model.ErrorArray.Count != 0)
        {
            @foreach (var error in Model.ErrorArray)
            {
                <div>
                    <p>@error.Message</p>
                </div>
            }
        }
    </div>
</div>